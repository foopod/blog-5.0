---
layout: layouts/wrapper.njk
title: Home
description: Jono Shields, software developer, maker and photographer
changefreq: daily
---

<section id="about">
    <p>Hi, I'm a software developer, a maker and a photographer.
         I love getting my hands dirty and trying new things, learning is the best feeling. 
          </p>
         <blockquote>Dude, suckin' at something is the first step to being sorta good at something.
             <span style="float:right">~ Jake the dog</span>
         </blockquote>
</section>
<section id="writing">
    <h1 class="section-heading">Recent</h1>
    <ul class="post-list">
        {%- for post in collections.post | reverse -%}
        {% if (loop.index <= 5) %}
        <li class="post-item">
            <div class="meta">
                <time datetime="{{ post.data.date.toISOString() }}" itemprop="datePublished">{{ post.data.date.toShortFormat() }}</time>
            </div>
            <span>
                <a href="{{ post.url}}">{{ post.data.title }}</a>
            </span>
        </li>
        {% endif %}
        {%- endfor -%}
        <li class="post-item">
            <div class="meta">
                <time itemprop="datePublished"></time>
            </div>
            <span>
                <a href="/archive">More</a>
            </span>
        </li>
    </ul>
</section>
<!--
<section id="projects">
    <h2 class="section-heading">GBA GameJam Log</h2>
    <ul class="project-list">
        {%- for post in collections.project | reverse -%}
        {% if (loop.index <= 5) %}
        <li class="project-item">
            <a href="{{ post.url}}">{{ post.data.title }}</a>: <p>{{ post.data.description }}</p>
        </li>
        {% endif %}
        {%- endfor -%}
    </ul>
</section>

-->